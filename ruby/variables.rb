system "clear"

# in variables its important to start with lowercase.
# if we define with higher case then its considered as a class in ruby
# use underscore "_" to difine the variables as its the standard way to declare the variable in ruby

first_name = "John Smith".upcase
print first_name